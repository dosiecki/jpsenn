apply plugin: 'java'
apply plugin: 'application'

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.google.code.gson:gson:2.7'
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

mainClassName = "dvolv.DVEnvironment"

run {
//    jvmArgs "-agentlib:hprof=cpu=times"
}
